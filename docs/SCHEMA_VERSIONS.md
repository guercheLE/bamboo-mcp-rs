# Versões e Fontes dos Esquemas — Bamboo

Este documento lista as versões, URLs de origem (Swagger/OpenAPI v3) e os arquivos gerados (`.db` e esquema `.json`, comprimido em `.zst`) usados na criação da base de dados vetorial do servidor MCP.

Versão padrão (`default_version`): **default**

| Versão | Arquivo .db | Arquivo de esquema | URL de origem |
|---|---|---|---|
| default | `mcp_store.db` | `src/validation/generated_schemas.json.zst` | https://dac-static.atlassian.com/server/bamboo/swagger.v3.json |
